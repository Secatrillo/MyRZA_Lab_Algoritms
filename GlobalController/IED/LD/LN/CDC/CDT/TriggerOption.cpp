#include "TriggerOption.h"

TriggerOption::TriggerOption(bool dchg_, bool qchg_, bool dupd_):
        dchg(dchg_), //change data
        qchg(qchg_), //quality change
        dupd(dupd_) //data update
        {}